spring.application.name=aws1_loan_ms


spring.datasource.url=jdbc:mysql://${DATABASE_HOSTNAME:svv-db.ctkkgqggcfmt.eu-west-2.rds.amazonaws.com}:${DB_PORT:3306}/${DB_NAME:Loan}
spring.datasource.username=${DB_USERNAME:admin}
spring.datasource.password=${DB_PASSWORD:admin123}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
server.port=${SERVER_PORT:7072}

spring.jpa.hibernate.ddl-auto=create
#spring.sql.init.mode=always
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect


management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always

bank.ms.base.url = ${BASE_URL:http://localhost:7071}